import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marbleminds.bubblecontacts',
  appName: 'Bubble Contacts',
  webDir: 'dist'
};

export default config;
